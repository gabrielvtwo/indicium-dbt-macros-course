
version: 2

models:
  - name: dim_customers
    description: "A starter dbt model"
    meta:
      owner: Maria
      steward: Kevin
      has_pii: false
    columns:
      - name: id
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null
